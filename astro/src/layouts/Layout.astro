---
// Design reference: https://sarthikg.com/
import Navbar from "../components/Navbar.astro";

// interface Props {
//     title: string;
// }

// const { title } = Astro.props;
---
<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Astro description" />
        <meta name="viewport" content="width=device-width" />
        <!-- <link rel="icon" type="image/svg+xml" href="/favicon.svg" /> -->
        <meta name="generator" content={Astro.generator} />
        <title>Seongtaek Lim's Portfolio</title>
    </head>
    <body>
        <Navbar />
        <div class="content flex-col-container">
            <slot />
        </div>
    </body>
</html>

<style is:global>
    /* Fonts */
    /* https://cdnfonts.com */
    @import url("https://fonts.cdnfonts.com/css/merliha");
    @import url("https://fonts.cdnfonts.com/css/sofia-pro");

    /* Icons */
    /* https://icon-sets.iconify.design/quill/ */
    /* e.g., <Icon name="filepath" /> */

    /* Page Transition */
    @view-transition {
        navigation: auto;
    }
    @keyframes blur-out {
        from {
            /* transform: scale(1); */
            filter: blur(0);
        }
        to {
            /* transform: scale(1.05); */
            filter: blur(10px);
        }
    }
    @keyframes blur-in {
        from {
            /* transform: scale(1.05); */
            filter: blur(10px);
        }
        to {
            /* transform: scale(1); */
            filter: blur(0);
        }
    }
    ::view-transition-old(root) {
        animation: 0.2s ease-in both blur-out;
    }
    ::view-transition-new(root) {
        animation: 0.2s ease-in both blur-in;
    }

    /* Global styles */
    :root {
        --accent: 117, 127, 154;
        --accent-light: 215, 221, 232;
        --accent-dark: 80, 85, 99;
        --accent-gradient: linear-gradient(
            20deg,
            rgb(var(--accent)),
            rgb(var(--accent-light)),
            white 40%
        );
        --background--dark: #1b1c1d;
        transition: opacity 3s ease-in-out;
    }
    body {
        font-family: "Sofia Pro", Tahoma, Arial, sans-serif;
        background: var(--background--dark);
        color: #fffff0;
        margin: 0;
        width: 100vw;
        height: 100vh;
        overflow: hidden;
    }
    .content {
        margin: 0;
        width: 100%;
        height: calc(100vh - 6rem);
        justify-content: center;
    }
    h1,
    h2,
    h3,
    h4,
    h5 {
        font-family: "Merliha", Garamond, Georgia, serif;
        letter-spacing: 0.2em;
    }
    p {
        font-weight: 200;
        letter-spacing: 0.1em;
    }
    .flex-col-container {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .flex-row-container {
        display: flex;
        flex-direction: row;
        align-items: center;
    }
</style>
